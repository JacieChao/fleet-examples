namespace: migration-demo
targetCustomizations:
- name: test
  clusterSelector:
    matchLabels:
      env: uat
  yaml:
    overlays:
    - servicelb

- name: prod
  clusterSelector:
    matchLabels:
      env: prod
  yaml:
    overlays:
    - scale3
